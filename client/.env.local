# Pryntis Panel — Local Development
# The Vite proxy in vite.config.js forwards /api -> http://localhost:5000
# so the frontend uses relative paths (/api/v1/...) by default.
# Only set this if your backend runs on a non-standard host/port.
VITE_API_URL=/api/v1
